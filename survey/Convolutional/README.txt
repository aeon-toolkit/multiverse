Notes
- Xiao2022DSN.csv uses the multivariate benchmark table (3 UCI datasets) and includes accuracy only.
- Zhang2023Adacket.csv uses the paper's resource-efficiency benchmark table on 3 representative UEA datasets; the PDF does not include a full per-dataset 30-dataset accuracy table.
- Zhang2025DM-FCN.csv uses the paper's main benchmarking summary table (TSER, UEA, UCR). The PDF reports archive-level summary metrics rather than a full per-dataset UEA table.
- Wang2024AdaFSNet.csv uses the multidimensional UEA results table shown in the PDF.
