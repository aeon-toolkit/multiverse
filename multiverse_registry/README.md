# Multiverse dataset registry

This folder contains the authoritative dataset registry and supporting notes.

- `mtsc_registry.csv` is the primary registry used to resolve Zenodo artefacts, checksums, licences, and notes.
- For releases, a snapshot of the registry should be copied into `src/multiverse/datasets/mtsc_registry.csv`.

Recommended columns are documented in `mtsc_registry.csv`.
